/**
 * Gateway Example: Proxy Keys
 */
import { AICostTracker } from 'cost-katana';
import { config } from '../../shared/config';

async function main() {
  console.log('\n🔑 Gateway Proxy Keys Example\n');
  console.log('Create proxy keys at: https://costkatana.com/settings/proxy-keys\n');
  console.log('Benefits: Hide real keys, set budgets, rate limiting\n');
}

main().catch(console.error);
